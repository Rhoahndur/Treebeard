# Railway Configuration for TreeBeard

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn src.backend.api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
