# TreeBeard Production Environment Variables
# Copy these to Railway's environment variables dashboard

# === Application ===
ENVIRONMENT=production
DEBUG=false
APP_NAME=TreeBeard Energy Recommendation API
APP_VERSION=1.0.0

# === Database ===
# Railway will provide this automatically when you add PostgreSQL service
# DATABASE_URL=postgresql://user:password@host:port/database

# === Security ===
# Generate a secure secret key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-production-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# === CORS ===
# Update these with your actual frontend URLs after deploying
CORS_ORIGINS=["https://your-frontend-domain.railway.app","https://your-custom-domain.com"]

# === OpenAI ===
# Required for AI-powered explanations
OPENAI_API_KEY=sk-proj-your-actual-openai-key-here
OPENAI_MODEL=gpt-4o-mini

# === Redis (Optional) ===
# Railway can provide this if you add Redis service
# REDIS_URL=redis://default:password@host:port

# === Monitoring (Optional) ===
MONITORING_ENABLED=false
SENTRY_DSN=
LOG_LEVEL=INFO

# === Rate Limiting ===
RATE_LIMIT_PER_USER=1000/minute
RATE_LIMIT_PER_IP=10000/hour

# === Cache Settings ===
CACHE_TTL_SECONDS=86400
RECOMMENDATION_CACHE_TTL_SECONDS=86400
