# Railway Configuration for TreeBeard Backend
# Note: Root Directory is set to src/backend in Railway UI

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash scripts/startup.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
