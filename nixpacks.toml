# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = [
  "cd src/backend",
  "pip install -r requirements.txt"
]

[start]
cmd = "cd src/backend && python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"
