# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = ["pip install -r src/backend/requirements.txt"]

[start]
cmd = "python -m uvicorn src.backend.api.main:app --host 0.0.0.0 --port $PORT"
